# 婚禮照片網站｜管理員操作說明

這份說明是寫給**網站管理員與內容編輯者**。

**不用懂程式。先改一小部分、儲存、再到前台確認。**

> 親友／賓客請看 [`EASY_USER_GUIDE.md`](EASY_USER_GUIDE.md)。  
> Replit、Drive、migration 與發布請看 [`OPERATIONS_GUIDE.md`](OPERATIONS_GUIDE.md)。

## 先記住六件事

1. **大部分設定要按頁面底部的「儲存所有變更」。**
2. **上傳、附件處理與永久刪除進行中時，不要關掉頁面。**
3. **永久刪除不能復原。**
4. **不要直接去 Google Drive 刪照片。**
5. **不要把管理密碼放進 GitHub、聊天或公開文件。**
6. **每次只改一小部分，存好後到前台確認。**

## 快速找功能

| 想做的事 | 到哪裡 |
| --- | --- |
| 改首頁背景、標題、配色 | 通用 → 樣式與首頁首圖 |
| 改日期、說明與其他公開文字 | 通用 → 其他網站文字 |
| 改瀏覽器分頁／手機主畫面圖示 | 通用 → 網站圖示 |
| 改流程按鈕或輪盤、開啟循環 | 通用 → 子分類操作方式 |
| 改媒體出現順序 | 通用 → 媒體順序 |
| 改訪客／管理員可選張數與上傳說明 | 通用 → 上傳方式 |
| 新增或修改相簿 | 相簿 |
| 管理訪客最新／全部／姓名標籤 | 相簿 → 訪客上傳 → 訪客相簿標籤 |
| 新增流程、影片、文章、附件、置頂照片 | 分類與影片 |
| 篩選、上傳、編輯或批次處理照片 | 照片 |
| 重掃 Drive、清縮圖、重建衍生圖 | 重新整理原始照片 |

---

## 1. 登入管理後台

打開：

```text
/Memories/admin/login
```

輸入管理員密碼。密碼來源是 Replit Secret：

```text
MEMORIES_ADMIN_TOKEN
```

登入狀態大約維持 30 分鐘。過期後重新登入即可。

不要在公用電腦保持登入；完成後按 **登出**。

---

## 2. 認識四個管理分頁

- **通用**：外觀、文字、網站圖示、上傳、媒體與子分類操作
- **相簿**：相簿、排序、顯示狀態與訪客姓名標籤
- **照片**：篩選、上傳、編輯、批次修改與永久刪除
- **分類與影片**：婚禮流程、YouTube、文章、附件與置頂照片

有些卡片可以按標題展開或收起。文件中稱為「收合區塊」。

---

## 3. 儲存變更

### 一般設定

修改後，頁面底部會顯示未儲存數量。

按：

**儲存所有變更**

看到成功訊息後才算完成。

目前下列內容都會加入全域儲存：

- 首頁樣式與首圖
- 網站主標題
- 其他網站文字
- 網站圖示
- 子分類操作與輪盤循環
- 上傳方式與張數限制
- 相簿、分類及一般照片草稿

### 不等全域儲存的操作

以下操作有自己的流程，依畫面完成訊息為準：

- 照片上傳
- 附件上傳或移除
- 永久刪除
- 重新掃描與縮圖維護

### 離開警告

看到「尚有未儲存的變更」時，先按取消，回去儲存。

儲存失敗時不要立刻關頁：

1. 讀錯誤訊息。
2. 檢查網路。
3. 再按一次儲存。
4. 仍失敗時保留分頁，交給維運者排查。

---

## 4. 修改首頁首圖、主標題與配色

進入：

**通用 → 樣式與首頁首圖**

這張卡片負責：

- 中文網站主標題
- English title
- 首頁背景圖片
- 背景遮罩透明度
- 整體頁面配色
- 首頁標題、日期與說明顏色
- 底部導覽背景、文字與選取顏色

### 首頁背景圖片

支援：

- PNG
- JPG／JPEG
- WebP
- 最大 10 MB

建議使用 **1600 × 900 或更大的 16:9 橫式圖片**。

儲存時系統會：

1. 讀取圖片方向
2. 置中裁切
3. 轉成 1600 × 900 WebP

選好圖片後可以先看本機預覽。按「儲存所有變更」前，公開網站不會正式更新。

### 遮罩透明度

範圍是 0%～95%。

數值越高：

- 背景越淡
- 文字通常越容易閱讀

### 網站主標題

主標題現在在這張卡片管理，不在「其他網站文字」。

中文與英文都可以按 Enter 換行，前台會保留行數。

### 套用預設樣式

按「套用預設樣式」只會建立待儲存草稿。

確認預覽後，再按頁面底部的「儲存所有變更」。

---

## 5. 修改其他網站文字

進入：

**通用 → 其他網站文字**

這裡修改：

- 日期
- 公開說明
- 相簿與上傳介面文字
- 其他中英文系統文案

網站主標題不在這裡，避免兩張卡片同時改到同一欄位。

按「套用預設文字」只會重設這張卡片負責的文字，不會改掉「樣式與首頁首圖」裡的主標題。

完成後按「儲存所有變更」。

---

## 6. 修改網站圖示

進入：

**通用 → 網站圖示**

這個圖示會用於：

- 瀏覽器分頁 favicon
- 手機加入主畫面的圖示

支援 PNG、JPEG、WebP，最大 **5 MB**。

建議使用：

- 正方形圖片
- 主體位於中央
- 有透明背景更好

系統會轉成 **192 × 192 PNG**。

可以：

- 選擇或更換圖示
- 移除自訂圖示
- 取消尚未儲存的變更

最後按「儲存所有變更」。部分瀏覽器會快取圖示，需要重新整理或重新開啟分頁才看得到。

---

## 7. 修改子分類操作與輪盤循環

進入：

**通用 → 子分類操作方式**

可選：

- 傳統按鈕
- 輪盤式選擇器

也可以調整輪盤同時顯示的項目數量。

### 每個相簿分開決定是否循環

可以分別設定：

- 婚禮流程 `wedding`
- 訪客上傳 `guest`

開啟循環後，輪盤滑到尾端會平順接回前端；鍵盤方向鍵也遵守相同規則。

關閉時則是有限輪盤。

修改後按「儲存所有變更」。

---

## 8. 修改媒體順序

進入：

**通用 → 媒體順序**

可以決定一個婚禮流程裡，這些群組的先後：

- 影片
- 文章
- 附件
- 置頂照片
- 一般照片

這是全站流程內容的群組順序。

相簿自己的照片排序，只會改「一般照片群組裡面」的照片順序，不會把影片或文章搬走。

---

## 9. 設定上傳方式、限制與說明

進入：

**通用 → 上傳方式**

可以設定：

- Google Drive 上傳模式
- 訪客每次可選張數
- 管理員每次可選張數
- 中文上傳說明
- 英文上傳說明

### 張數限制

訪客與管理員可以分開設定：

- 範圍：1～100 的整數
- 預設：訪客 10、管理員 30

前台與後台會顯示目前限制。

設定 100 不代表同時傳 100 張；系統仍限制同時工作數量。

### Drive 上傳模式

#### `single`

在 resumable session 中一次傳完整檔案。預設使用這個。

#### `chunked`

以 4 MiB 分段，保存 session 與已完成位置，適合較大檔案或不穩定網路測試。

已經開始的照片會保持它開始時的模式。

### 中英文上傳說明

可提醒親友：

- 支援格式
- 25 MB 限制
- 不要關掉頁面
- 新人指定的分類或命名方式

完成後按「儲存所有變更」。

---

## 10. 新增與編輯相簿

進入 **相簿**。

### 新增相簿

展開「新增相簿」，填寫：

1. 中文名稱
2. 英文名稱
3. 中文說明
4. 英文說明
5. 是否對訪客顯示

### 編輯相簿

展開相簿名稱，可以修改文字、顯示狀態與照片排序。

排序方式包括：

- 隨機
- 時間早到晚／晚到早
- 名稱 A→Z／Z→A
- 作者 A→Z／Z→A

相簿重新排序不會改變穩定網址。

完成後按「儲存所有變更」。

---

## 11. 管理訪客相簿標籤

進入：

**相簿 → 訪客上傳 → 訪客相簿標籤**

可分別開關：

1. 最新照片
2. 所有訪客
3. 姓名標籤

關閉只會隱藏導覽，不會刪照片。

### 最新照片張數

可設定最近 **30～50 張**，固定以最新在前顯示。

### 姓名順序

- 電腦：拖曳
- 手機：使用 `↑`、`↓`

新名字加在最後，不會自動按字母排序。

隱藏或不存在的標籤網址會回到訪客相簿；移動順序不會改網址。

---

## 12. 新增與編輯婚禮流程

進入 **分類與影片**。

新增流程時填寫：

- 中文名稱
- 英文名稱
- YouTube 連結（沒有可留空）
- 是否自動播放

流程排序使用 `↑`、`↓`。

重新排序只改畫面位置，不改穩定網址。

手機瀏覽器通常只允許靜音自動播放，這是正常限制。

---

## 13. 編輯文章、附件與置頂照片

在流程內容編輯區可以加入：

- 中文文章
- 英文文章
- 標題與段落
- Drive 附件
- 分隔空間
- 1～3 張置頂照片

附件上傳或移除時，等畫面顯示完成再離開。

置頂照片候選每頁 10 張。切頁時，隱藏頁面的縮圖請求可能停止，這是正常的省流量行為。

置頂照片會出現在流程文字後、一般照片前。

---

## 14. 篩選與查看照片

進入 **照片**。

可一起篩選：

- 相簿
- 流程
- 作者／上傳者

按「清除篩選」回到全部。

每頁顯示 10 張；桌面通常一列 5 張，窄畫面自動減少欄數。

批次操作只處理目前這一頁。

---

## 15. 上傳管理員照片

進入：

**照片 → 新增照片**

依序：

1. 輸入作者／上傳者
2. 選照片
3. 選至少一個相簿
4. 需要時選流程
5. 按一次上傳
6. 等每張完成

限制：

- 張數依通用設定，1～100，預設 30
- 每張最多 25 MB
- JPEG、PNG、WebP、HEIC、HEIF

看到「繼續未完成照片」時可繼續。

若原圖已成功但分類失敗：

1. 不要重複上傳。
2. 用篩選找到照片。
3. 補上相簿或流程。
4. 儲存。

---

## 16. 編輯單張照片

可以修改：

- 顯示名稱
- 拍攝時間
- 作者／上傳者
- 公開或隱藏
- 所屬相簿
- 流程分類

一張照片至少要屬於一個相簿。

- 公開：親友看得到
- 隱藏：只在後台管理，不是刪除

完成後按「儲存所有變更」。

---

## 17. 批次處理目前頁面的照片

先勾選照片，或按「全選目前結果」。

可以：

- 增加到所選相簿
- 取代為所選相簿
- 更改流程
- 批次修改作者／上傳者
- 永久刪除可刪除的照片

切到下一頁後，上一頁不會自動一起處理。

---

## 18. 永久刪除

> **永久刪除不能復原。**

會刪除：

- Drive 原圖
- Drive 縮圖
- 相簿與流程關聯
- 資料庫紀錄
- 置頂照片引用

同一底層照片出現在多個相簿時，永久刪除會處理整個照片家族，不只是目前那一張卡片。

作者為：

```text
婚禮攝影
```

的照片受到前端與伺服器保護，不能永久刪除；批次刪除會自動略過。

---

## 19. 重新掃描 Drive 與重建縮圖

展開「重新整理原始照片」。

可以針對相簿或流程：

1. 清除系統產生的縮圖
2. 重新掃描 Drive 原圖
3. 排入縮圖重建

使用前：

- 確認選對相簿或流程
- 確認 Drive 連線正常
- 不要連續重複按
- 等待完成

不需要時不要使用。

---

## 20. 不要直接從 Drive 刪照片

手動刪除 Drive 原圖，不會自動完整清理網站資料庫。

可能造成：

- 照片紀錄還在
- 原圖或縮圖打不開
- Drive 與資料庫不一致

要刪除請使用管理後台或上傳者私人管理頁。

---

## 21. 常見問題

### 儲存後前台沒變

1. 確認顯示成功訊息。
2. 重新整理前台。
3. 確認相簿／照片是公開狀態。
4. 確認改的是正確語言、相簿或流程。

### 首頁圖片或網站圖示沒更新

1. 確認已按「儲存所有變更」。
2. 重新整理公開網站。
3. 圖示可能被瀏覽器快取，重新開啟分頁再看。

### 後台突然回到前台

登入 session 可能過期。重新進入 `/Memories/admin/login`。

### 照片空白

1. 等一下並滑動頁面。
2. 檢查網路。
3. 重新整理。
4. 仍大量失敗時交給維運者檢查 Drive Integration 與 `系統縮圖` 權限。

### 上傳卡住

1. 不要重新選同一批。
2. 檢查網路。
3. 使用「繼續未完成照片」。

### 照片刪不掉

先看作者是否為 `婚禮攝影`。若是，這是保護機制。

---

## 最安全的工作方式

1. 一次只改一小部分。
2. 看預覽。
3. 按「儲存所有變更」。
4. 等成功訊息。
5. 到前台重新整理並確認。
6. 正確後再做下一部分。

## 管理員最重要的六件事

1. **知道哪些操作要全域儲存。**
2. **首頁主標題在「樣式與首頁首圖」，其他文案在「其他網站文字」。**
3. **永久刪除不能復原。**
4. **不要直接去 Drive 刪原圖。**
5. **上傳與維護進行中不要關頁。**
6. **做完一定到公開網站確認。**