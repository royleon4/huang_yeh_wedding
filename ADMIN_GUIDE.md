# 婚禮照片網站｜管理員超簡單操作說明

這份說明是寫給網站管理員。

**不用懂程式。看到什麼，就照著一步一步做。**

> 親友／賓客請看 [`EASY_USER_GUIDE.md`](EASY_USER_GUIDE.md)。

---

## 先記住四件事

1. **改完資料，要記得儲存。**
2. **永久刪除不能復原。**
3. **不要直接去 Google Drive 刪照片。**
4. **看到上傳或儲存進行中時，不要關掉頁面。**

---

## 1. 登入管理後台

### 第一步：打開登入頁

打開：

```text
/Memories/admin/login
```

### 第二步：輸入管理密碼

輸入管理員密碼。

這個密碼來自網站的 Replit Secret：

```text
MEMORIES_ADMIN_TOKEN
```

**不要把密碼寫進 GitHub、聊天室、公開文件或前端程式碼。**

### 第三步：按登入

密碼正確後，網站會進入管理後台。

登入大約會維持 **30 分鐘**。

超過時間後，網站可能會請你重新登入。

---

## 2. 認識管理後台

後台主要有這些區域：

- **通用**：網站文字、顯示方式、照片排列及上傳方式
- **相簿**：新增及編輯相簿；「訪客上傳」裡也有訪客相簿標籤設定
- **照片**：篩選、上傳、編輯、批次處理及刪除照片
- **分類與影片**：婚禮流程、影片及流程內容

有些區塊可以按標題打開或收起。

這種區塊叫做 **Accordion／收合區塊**。

看到小箭頭時，按一下標題就會展開。

---

## 3. 儲存變更

這是最重要的部分。

### 一般資料怎麼儲存

相簿、照片及分類等一般修改，會先留在畫面上。

修改完成後，找到頁面底部的：

**「儲存所有變更」**

再按一下。

看到成功訊息後，才算真的存好。

### 有自己的儲存按鈕時

少數區塊有自己的儲存按鈕，例如網站文字設定。

看到區塊內有清楚的 **「儲存」** 按鈕時，就使用那個按鈕。

### 離開前看到警告

如果網站說：

> 尚有未儲存的變更

代表你還沒有存好。

先按取消，回去儲存，再離開。

### 儲存失敗時

1. 不要重新輸入全部內容。
2. 先看畫面上的錯誤訊息。
3. 檢查網路。
4. 再按一次儲存。
5. 還是不行時，先不要關閉分頁。

網站會盡量保留尚未成功儲存的草稿。

---

## 4. 修改網站文字

進入 **通用**。

找到 **網站文字**。

你可以修改：

- 中文標題
- 英文標題
- 中文說明
- 英文說明
- 公開頁面的其他文字

### 標題要換行

直接在輸入框裡按 Enter 換行。

前台會照你的換行顯示。

### 修改完成

按區塊內的儲存按鈕。

重新整理前台，確認文字是否正確。

---

## 5. 修改網站顯示方式

進入 **通用**。

你可以設定流程選擇方式，例如：

- 傳統按鈕
- 輪盤式選擇器

也可以設定輪盤同時看到幾個項目。

修改後記得儲存。

### 全域媒體順序

這裡可以決定一個婚禮流程裡，內容出現的先後順序，例如：

- 影片
- 文章
- 附件
- 置頂照片
- 一般照片

這個順序是整體規則。

相簿自己的照片排序，只會改變「照片群組裡面」的順序，不會把影片或文章的位置打亂。

---

## 6. 設定照片上傳方式

進入 **通用**，找到 **上傳方式** 卡片。

這裡可以一起設定：

- Google Drive 上傳模式
- 訪客每次最多選幾張
- 管理員每次最多選幾張
- 中文上傳說明
- 英文上傳說明

### 設定可選照片數量

訪客與管理員的數字可以分開設定。

- 可輸入 **1～100** 的整數
- 預設是訪客 **10 張**、管理員 **30 張**
- 例如可以輸入 17、50 或 82
- 不要輸入小數、0、負數或超過 100

前台與後台的選擇按鈕會顯示目前限制。

即使設定成 100 張，網站也不會同時傳送 100 張；它仍會限制同時進行的工作數量。

### 自訂上傳說明

可以分別輸入中文與英文說明。

這段文字會顯示在選擇照片的位置下面。

可以用來提醒：

- 支援哪些格式
- 每張檔案大小限制
- 上傳時不要關閉頁面
- 其他新人想告訴賓客的事情

### Drive 上傳模式

目前有兩種：

#### `single`

一次傳送整張照片。

這是預設模式。

#### `chunked`

把照片分成 4 MiB 小段上傳。

網路中斷後，可以從已完成的位置繼續。

### 不確定選哪一個

保持 `single`。

除非正在測試較大的照片或不穩定網路，才改成 `chunked`。

已經開始上傳的照片，會繼續使用它開始時的模式。

修改完成後，按頁面底部的 **「儲存所有變更」**。

---

## 7. 新增相簿

進入 **相簿**。

找到 **新增相簿**，按標題展開。

依序填寫：

1. 中文名稱
2. 英文名稱
3. 中文說明
4. 英文說明
5. 是否對訪客顯示

填好後，按頁面底部的 **「儲存所有變更」**。

### 相簿看不到

檢查：

- 是否已勾選「對訪客顯示」
- 是否按過「儲存所有變更」
- 前台是否已重新整理

---

## 8. 編輯相簿與訪客相簿標籤

進入 **相簿**。

每個相簿都有自己的收合區塊。

按相簿名稱展開。

你可以修改：

- 中文名稱
- 英文名稱
- 中文說明
- 英文說明
- 是否對訪客顯示
- 相簿照片排序方式

### 可用的照片排序

相簿可以依下列方式排列照片：

- 隨機
- 時間由早到晚
- 時間由晚到早
- 照片名稱 A 到 Z
- 照片名稱 Z 到 A
- 作者 A 到 Z
- 作者 Z 到 A

### 設定訪客相簿標籤

展開：

**相簿 → 訪客上傳 → 訪客相簿標籤**

這個區塊預設是收起來的，再按一次標題才會展開。

裡面有三個可以各自勾選的設定：

1. **顯示最新照片標籤**
2. **顯示所有訪客標籤**
3. **顯示姓名標籤**

這三個設定互相獨立。例如：

- 可以只顯示「最新照片」和姓名
- 可以只顯示「所有訪客」
- 也可以三個都顯示

關閉一種標籤只會隱藏那一種選項，不會刪除照片。

### 設定最新照片張數

勾選「顯示最新照片標籤」後，可以設定最近 **30～50 張**。

取消勾選時，張數輸入框會停用。

### 調整姓名標籤順序

- 電腦：拖動姓名標籤
- 手機：使用 `↑`、`↓`

新出現的名字會加在目前順序最後，不會依姓名自動重排。

相簿或標籤移動位置後，穩定網址不會改變。標籤被刪除或隱藏而不可使用時，舊網址會被視為找不到，再導向訪客相簿。

修改後按 **「儲存所有變更」**。

---

## 9. 新增婚禮流程／分類

進入 **分類與影片**。

找到新增分類區塊。

填寫：

1. 中文分類名稱
2. 英文分類名稱
3. YouTube 連結（沒有就留空）
4. 是否自動播放

儲存後，新的流程會出現在列表中。

### 自動播放注意事項

手機瀏覽器通常只允許靜音自動播放。

所以勾選自動播放時，影片可能會先靜音播放，這是正常的。

---

## 10. 修改婚禮流程順序

進入 **分類與影片**。

每個分類標題會顯示：

- 編號
- 中文名稱
- 英文名稱

展開要修改的分類。

使用：

- `↑` 往前移
- `↓` 往後移

排好後按 **「儲存所有變更」**。

流程順序只會改變畫面上的排列。公開網址綁定流程本身的穩定身分，不會因為移到前面或後面而改變。

---

## 11. 加入或修改流程影片

進入 **分類與影片**。

展開一個流程。

在 **YouTube 連結** 中貼上完整網址，例如：

```text
https://www.youtube.com/watch?v=...
```

需要時勾選自動播放。

修改後儲存。

### 影片沒有出現

檢查：

1. 網址是否完整。
2. 影片是否可公開播放。
3. 是否已儲存。
4. 前台是否已重新整理。

---

## 12. 編輯流程文章與附件

在流程內容編輯區，可以加入：

- 中文文章
- 英文文章
- 標題
- 段落
- 附件
- 分隔空間

文章使用文字編輯器。

操作方式和一般文書軟體類似。

### 附件

附件的檔案會放在 Google Drive。

附件資料會記錄在網站資料庫。

上傳或移除附件時，等待畫面顯示完成再離開。

---

## 13. 選擇置頂照片

每個婚禮流程可以選 **1 到 3 張**置頂照片。

置頂照片會在該流程文字內容下方、一般照片之前顯示。

### 選擇方法

1. 進入流程內容編輯區。
2. 找到置頂照片。
3. 用上一頁／下一頁瀏覽候選照片。
4. 勾選想要的照片。
5. 最多選三張。
6. 調整順序。
7. 儲存。

每頁顯示 10 張候選照片。

切換頁面後，網站會停止載入已經看不到的縮圖，這是正常的省流量設計。

---

## 14. 查看與篩選照片

進入 **照片**。

可以一起使用三種篩選：

- 相簿
- 流程分類
- 作者／上傳者

選好條件後，網站會顯示符合的照片。

要重新開始時，按 **清除篩選**。

### 照片分頁

每頁顯示 10 張照片。

桌面寬畫面通常一列 5 張。

手機或較窄畫面會自動變成較少欄。

使用 **上一頁** 和 **下一頁** 瀏覽。

---

## 15. 上傳管理員照片

進入 **照片**。

展開 **新增照片**。

### 填寫資料

1. 輸入上傳者／作者。
2. 選擇照片。
3. 選擇至少一個相簿。
4. 需要時選擇婚禮流程分類。
5. 按上傳。

### 上傳限制

- 一次最多張數依「通用 → 上傳方式 → 管理員每次最多上傳」設定
- 可設定 1～100 張，預設是 30 張
- 每張最多 **25 MB**
- 支援 JPEG、PNG、WebP、HEIC、HEIF

### 上傳時

- 不要關閉頁面
- 不要重複按上傳
- 等待每張照片顯示完成

看到 **繼續未完成照片** 時，可以按下去繼續。

### 照片已上傳，但分類失敗

有時原圖已經成功上傳，但相簿或流程分類還沒有完成。

看到相關錯誤時：

1. 不要重新上傳同一張照片。
2. 使用照片篩選找到它。
3. 編輯它的相簿或流程。
4. 再儲存。

---

## 16. 編輯單張照片

進入 **照片**。

找到照片後，可以修改：

- 顯示名稱
- 拍攝時間
- 作者／上傳者
- 公開或隱藏
- 所屬相簿
- 流程分類

一張照片至少要屬於一個相簿。

修改後按 **「儲存所有變更」**。

### 公開與隱藏

- **公開**：訪客可以看到
- **隱藏**：只有管理員可以管理，公開網站不顯示

隱藏不是刪除。

---

## 17. 批次處理照片

批次操作只會處理 **目前這一頁** 的照片。

### 選取照片

1. 勾選想處理的照片。
2. 或按「全選目前結果」。

### 可以做的事情

- 保留目前相簿
- 增加到所選相簿
- 改成所選相簿
- 保留目前流程分類
- 更改流程分類
- 批次修改上傳者／作者名稱
- 永久刪除

批次修改上傳者時，輸入一次新名稱，再套用到所有選取照片。名稱會整理前後空白。

### 完成後

按執行按鈕，等待結果訊息。

切到下一頁後，上一頁的照片不會自動一起被處理。

---

## 18. 永久刪除照片

> **非常重要：永久刪除不能復原。**

永久刪除會移除：

- Google Drive 原圖
- Google Drive 縮圖
- 所有相簿關聯
- 所有流程關聯
- 資料庫紀錄
- 置頂照片引用

### 刪除單張照片

1. 找到照片。
2. 按 **永久刪除**。
3. 閱讀確認訊息。
4. 確定真的不要了，才按確認。

### 刪除同一張照片的所有紀錄

同一底層照片可能出現在多個相簿或流程中。

永久刪除時，網站會一起刪除這張底層照片的整個照片家族，而不是只移除目前看到的一份關聯。

### 婚禮攝影照片

作者為：

```text
婚禮攝影
```

的照片受到保護。

前端和伺服器都不允許永久刪除。

批次刪除時，這些照片會自動略過。

---

## 19. 重新整理原始照片

這是維護工具。

進入後台，展開 **重新整理原始照片**。

它可以針對相簿或流程：

1. 清除網站產生的縮圖。
2. 重新掃描 Google Drive 原圖。
3. 重新建立縮圖。

它不應該刪除原始照片。

### 使用前

- 確認選對相簿或流程
- 確認 Google Drive 連線正常
- 不要連續重複按
- 等待操作完成

這是較高風險的維護功能，不需要時不要使用。

---

## 20. 不要直接從 Google Drive 刪照片

手動刪除 Drive 原圖，不會自動完整清除網站資料庫。

這可能造成：

- 網站還看得到照片紀錄
- 縮圖或原圖打不開
- 資料庫與 Drive 不一致

需要刪除照片時，請使用：

- 管理後台的永久刪除
- 上傳者的私人管理頁

---

## 21. 相簿排序和媒體順序有什麼不同

### 相簿照片排序

只改照片群組裡面的照片順序。

例如：

- 由早到晚
- 由晚到早
- 依名稱
- 依作者
- 隨機

### 全域媒體順序

決定影片、文章、附件、置頂照片和一般照片群組誰先出現。

所以：

**先設定內容群組的位置，再設定照片群組裡面的照片順序。**

---

## 22. 登出

完成後，按後台的 **登出**。

如果離開很久，系統也會讓登入狀態到期。

不要在公用電腦上保持登入。

---

## 23. 常見問題

### 按了儲存，但前台沒變

1. 看是否顯示儲存成功。
2. 重新整理前台。
3. 確認相簿或照片是公開狀態。
4. 確認選對相簿或流程。

### 訪客標籤沒有出現

1. 進入「相簿 → 訪客上傳 → 訪客相簿標籤」。
2. 確認對應的三個顯示選項有勾選。
3. 按「儲存所有變更」。
4. 重新整理前台。
5. 姓名標籤仍沒有時，確認該姓名目前有訪客相簿照片。

### 照片是空白的

1. 等一下。
2. 往上或往下滑一點。
3. 檢查網路。
4. 重新整理頁面。
5. 確認 Google Drive Integration 仍有權限。

### 後台突然回到前台

管理員登入可能已經過期。

重新進入 `/Memories/admin/login` 登入。

### 上傳卡住

1. 不要重複重新選照片。
2. 檢查網路。
3. 看是否有「繼續未完成照片」。
4. 有的話按下去。

### Google Drive 同步失敗

檢查：

- Replit Google Drive Integration 是否仍連線
- 使用的 Google 帳號是否正確
- 該帳號是否能編輯婚禮根資料夾
- 是否能讀寫 `系統縮圖` 資料夾

### 照片刪不掉

先看作者是不是 `婚禮攝影`。

若是，這是保護機制，不是錯誤。

---

## 24. 最安全的操作順序

每次修改時，照這個順序：

1. 先只改一小部分。
2. 按儲存。
3. 等待成功訊息。
4. 打開前台確認。
5. 正確後再做下一部分。

不要一次改很多重要設定，再一起猜是哪裡出錯。

---

## 管理員最重要的五件事

1. **改完要儲存。**
2. **永久刪除不能復原。**
3. **不要直接去 Drive 刪原圖。**
4. **上傳或維護時不要關掉頁面。**
5. **做完後到前台確認。**