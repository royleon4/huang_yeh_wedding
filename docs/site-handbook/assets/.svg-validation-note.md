# SVG rendering validation

The README architecture diagram previously contained an unescaped ampersand in XML text content. The source now uses `&amp;`, and README image references request raw rendering to avoid stale proxy behavior.

Validated with an XML/SVG renderer before merge.
